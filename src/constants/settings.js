const API_URL = 'https://jsonplaceholder.typicode.com/';

const RESOURCES = {
  POSTS: 'posts',
  COMMENTS: 'comments',
  ALBUMS: 'albums',
  PHOTOS: 'photos',
  TODOS: 'todos',
  USERS: 'users',
};

const BUTTONS_CODE = {
  ESCAPE: 27,
};

export { RESOURCES, API_URL, BUTTONS_CODE };
